export const metadata = {
  title: 'EVERY AI',
  description: 'Ai 공유 플랫폼',
}

export default function RootLayout({ children }) {
 return (
    <html lang="kr">
      <body>{children}</body>
    </html>
  )
}
